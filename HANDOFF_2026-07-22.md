# EEGLAB / ERPLAB / N400 项目交接摘要（2026-07-22）

## 1. 原始目标与验收标准

原始目标：将工作区的 `Full.pdf` 建设为长期可维护、页码可追溯的 EEGLAB、ERPLAB、N400 知识库与 Codex Skill，并用该 Skill 分阶段、手把手分析 Neuroscan/CURRY 原始 EEG。

验收标准：

1. 不一次性加载整本 PDF；本地提取，必要时 OCR。
2. 保留章节、标题、图表说明和物理页码；拆分至 `knowledge/chapters/`。
3. 建立 `knowledge/index.md`；逐页原文是事实依据，保留 `Full.pdf`。
4. Skill 包含简洁入口、SOP、参数决策表、GUI/MATLAB 对照、配置化脚本、QC、报告模板和原书页码。
5. 详细知识进入 `references/`；`SKILL.md` 不塞入整书摘录。
6. 建议必须标记 `[BOOK]`、`[DERIVED]`、`[GENERAL]`、`[DECIDE]`、`[UNVERIFIED]`；不能确认的命令/参数不得猜测。
7. 滤波、基线、N400 窗口、电极等不得固化为普适唯一值；配置集中管理。
8. 记录软件版本差异，每阶段设置 QC gate。
9. 实际分析先单个代表性被试 pilot，禁止直接批处理。

## 2. 当前已完成工作

### 2.1 PDF 知识库

- 原始 PDF：`/Users/kevinchain/Documents/Codex/eeg-n400-analysis-skills/Full.pdf`，保留。
- PDF 共 374 个物理页；已完成本地文本提取。
- 逐页事实底稿：`knowledge/raw/pages.jsonl`，374 条记录。
- 章节：`knowledge/chapters/00-front-matter.md` 至 `15-back-matter.md`，共16份。
- 索引：`knowledge/index.md`。
- 提取报告：`knowledge/extraction_report.md`。
- 提取脚本：`scripts/extract_pdf_knowledge.py`。

### 2.2 Codex Skill

Skill：`analyze-eeglab-erplab-n400/`。

- `SKILL.md`：证据标签在第10–18行；分阶段流程和 guardrails 在第20–39行。
- `references/sop.md`：import gate 第26–34行；pre-ICA 第36–45行；ICA 第47–57行。
- `references/parameter-decisions.md`：参考、滤波、降采样、ICA、epoch、N400和统计决策集中在第7–28行。
- `references/gui-matlab-crosswalk.md`：GUI/命令对照；不确定调用必须从本机 history/help 确认，第29–35行。
- `references/quality-control.md`：全阶段 QC。
- `references/n400-design-scoring-statistics.md`：N400设计、测量、差异波和统计。
- `references/source-map.md`：证据标签第7–15行；高价值原书结论第37–45行。
- `references/version-compatibility.md`：原书软件时代第5–9行；2026状态/警告第11–15行。
- `scripts/config_template.m`、`validate_config.m`、`run_pipeline.m`：三阶段模板。
- 阶段仅限 `preica`、`train_ica`、`postica_erp`：`config_template.m:6`、`validate_config.m:4`、`run_pipeline.m:7-8,32-41`。
- `assets/methods-report-template.md`、`parameter-decision-ledger.csv`、`participant-decisions.csv`：报告与决策账本。
- `agents/openai.yaml`：Skill 元数据。

### 2.3 01A 实际导入审计

项目：`/Users/kevinchain/Desktop/N400_project/`。

已清点6名：01A、01B、02A、02B、03A、03B；每名有同名 `.cdt + .cdt.ceo + .cdt.dpo`，不得拆散或覆盖。

代表文件：
`/Users/kevinchain/Desktop/N400_project/raw/01A_EEG_data/Acq 2026_07_01_1824.cdt`

软件：

- macOS 15.6；MATLAB R2026a Update 3（26.1.0.3276743）。
- EEGLAB 2026.0.0：`/Users/kevinchain/Documents/MATLAB/eeglab2026.0.0/eeglab.m`。
- ERPLAB 13.00：`pop_creabasiceventlist`、`pop_binlister`、`pop_averager` 可定位。
- loadcurry 3.3.2：`plugins/loadcurry3.3.2/pop_loadcurry.m`、`loadcurry.m`。

正式导入命令：

```matlab
EEG = loadcurry( ...
    '/Users/kevinchain/Desktop/N400_project/raw/01A_EEG_data/Acq 2026_07_01_1824.cdt', ...
    'KeepTriggerChannel', 'True', ...
    'CurryLocations', 'True');
```

审计结论：

- 1000 Hz；67通道（64 EEG + VEOG + HEOG + Trigger）；3642200 samples；连续1 trial；60.703 min。
- `.dpo` 报告单位 `uV`；采集模式 DC。
- 标签完整且无重复。CPz=39、Cz=30、Pz=50、M1=44、M2=45、VEOG=65、HEOG=66、Trigger=67。
- 坐标来源：`metadata/SynAmpsRT - Quik-Cap Net 64_SPIN.xml` 与厂家模板 `metadata/64 -新电极帽.DAT`。
- `CurryLocations=False` 时 60/67 有位置，缺 F11/F12/FT11/FT12/CB1/CB2/Trigger。
- `CurryLocations=True` 时 64/67 有位置，仅 VEOG/HEOG/Trigger 无 XYZ，符合预期。
- 厂家 DAT 与 XML/DPO 经轴转换一致；是模板而非个体数字化坐标。可用于传感器级 ERP、拓扑和插值，不得宣称个体定位或精确源定位。
- 拓扑人工通过：鼻尖向上；左右和中线关系正确；无镜像/旋转。
- 频谱确认强烈窄带 50 Hz 工频；低频因 DC 漂移升高。尚未滤波、陷波或 CleanLine。
- 去中位数显示确认通道有信号，但存在工频和共同瞬态；尚未删通道/时段。

### 2.4 事件与研究设计

2（Constraint：HC/LC）×5（SNR：-4、-2、+4、+6、Quiet），10条件。

- 句子 onset：HC 11–15；LC 21–25。
- 目标词 onset：HC 111–115；LC 121–125。
- N400 必须锁定目标词 111–125，不能用句子 onset 加固定延迟推算。
- 01A 总事件602：300句子 + 300目标词 + 99/98各1。
- 20种实验事件各30次。
- 全量配对：600实验事件、300 trial、`Invalid pairs: 0`；目标码=句子码+100。
- 延迟：N=300；915–2749 ms；mean 1606.6；median 1583.5；SD 299.8；P01/P05/P25/P50/P75/P95/P99=1003/1118/1410.5/1583.5/1806/2101/2247.5 ms。
- 99=约4 min静息开始，98=静息结束；不进入 N400 BinList。
- 现有 `events/BDF_HC_LC_SNR.txt` 使用句子码，不适合作为目标词 N400 最终 BinList；目标词 BinList 尚未写盘。

### 2.5 已固定决策

- 在线参考：厂家配置中的专用 `REF.`，未作为普通通道保存；M1/M2 是独立记录通道，不是在线参考。
- 主分析保留10条件，统计表达 Constraint、SNR及交互，不做45个无计划两两比较。
- 主 ERP：Correct-only 且 EEG trial 无伪迹。
- 补充/敏感性：all-trial，但仍排除 EEG 伪迹。
- 错误 trial 不得在连续预处理/ICA前删除。顺序：全连续数据ICA→清理→目标词epoch→EEG伪迹→行为对齐→Correct-only主分析/all-trial敏感性。

## 3. 每个已生成或维护文件及目的

当前目录不是 Git 仓库；无法以 commit 区分原文件和改动。完整事实产物如下。

### 根目录/提取

- `Full.pdf`：原始事实依据。
- `scripts/extract_pdf_knowledge.py`：逐页提取、拆分、索引。
- `knowledge/raw/pages.jsonl`：374页逐页事实底稿。
- `knowledge/index.md`：主题检索。
- `knowledge/extraction_report.md`：提取和追溯说明。

### 章节

- `00-front-matter.md`：前置内容。
- `01-first-steps.md`：环境与入门。
- `02-single-participant-n400.md`：单被试 N400。
- `03-multiple-participant-n400.md`：多被试。
- `04-filtering.md`：滤波与失真。
- `05-referencing-channel-operations.md`：参考/通道。
- `06-bins-averaging-baseline-data-quality.md`：bins/平均/基线/QC。
- `07-eeg-inspection-bad-channels.md`：连续检查/坏道。
- `08-artifact-detection-rejection.md`：伪迹检测/拒绝。
- `09-ica-artifact-correction.md`：ICA。
- `10-scoring-statistics.md`：测量/统计。
- `11-scripting.md`：脚本。
- `12-appendix-eeg-erp-introduction.md`：EEG/ERP基础。
- `13-appendix-troubleshooting.md`：排错。
- `14-appendix-processing-pipeline.md`：完整管线。
- `15-back-matter.md`：后置内容。

### Skill

- `SKILL.md`：入口、证据分类、执行 guardrails。
- `agents/openai.yaml`：元数据。
- `references/sop.md`：阶段 SOP/gates。
- `references/parameter-decisions.md`：决策及页码。
- `references/gui-matlab-crosswalk.md`：GUI/命令对照。
- `references/quality-control.md`：QC。
- `references/n400-design-scoring-statistics.md`：N400/统计。
- `references/source-map.md`：证据/页码。
- `references/version-compatibility.md`：兼容性。
- `scripts/config_template.m`：集中配置；示例均为 `[DECIDE]`。
- `scripts/validate_config.m`：阶段配置校验。
- `scripts/run_pipeline.m`：三阶段模板，ICA审核是人工 gate。
- `scripts/validate_templates.py`：静态模板检查。
- `assets/methods-report-template.md`：报告模板。
- `assets/parameter-decision-ledger.csv`：研究级决策。
- `assets/participant-decisions.csv`：被试级决策。

### 非研究产物

- `tmp/pdfs/render/*`：PDF渲染抽检。
- `__pycache__/`、`.DS_Store`：缓存。
- 本文件 `HANDOFF_2026-07-22.md`：续接摘要。

## 4. 当前代码状态与关键实现

- Skill 静态结构完整，详细内容已分流至 references。
- `run_pipeline.m` 只接受三阶段；`validate_config.m:4`。
- preica 输出 `<subject>_preica.set`（`run_pipeline.m:55-83`）。
- ICA组件识别必须人工审核，不能自动猜测。
- postica_erp 从 pre-ICA 数据应用已审核决策并生成 ERP/评分（入口约 `run_pipeline.m:124-125`）。
- Vendor import、坏段人工审查、行为对齐未集成；CURRY必须使用已验证 loadcurry，不能换 BIOSIG/CNT。
- `config_template.m` 的250 Hz、0.1–30 Hz、-200–800 ms、300–500 ms仅为 `[DECIDE]` 示例（第41、45–46、84–85、114行）。
- 容器无 MATLAB/Octave；脚本仅静态验证。用户本机只完成 import/audit。
- 尚未正式滤波、重参考、降采样、删坏道/坏段、ICA、EventList/BinList、epoch、伪迹检测或平均。
- 用户未明确回报 canonical `input_set/01A_imported.set` 是否成功保存且含 Curry 坐标；恢复时先确认。

## 5. 已运行命令与结果

```bash
python3 analyze-eeglab-erplab-n400/scripts/validate_templates.py
```

结果：`Static template checks passed. MATLAB/EEGLAB runtime validation is still required.`

先前官方 Skill validator 通过，但本交接阶段未重新定位该 validator。

```bash
git status --short
git log -1 --oneline --decorate
```

均返回：`fatal: not a git repository (or any of the parent directories): .git`。

插件检测最终成功：`which pop_loadcurry -all`、`which loadcurry -all`、`which pop_creabasiceventlist -all`、`which pop_binlister -all`、`which pop_averager -all`。

已解决失败：

- 初次：`未找到 'pop_loadcurry'。`、`未找到 'loadcurry'。`；安装3.3.2解决。
- 初次启动前：`未找到 'pop_averager'。`；加载 ERPLAB 13 解决。
- EEGLAB提示 `Cannot connect to the Internet to retrieve statistics for extensions`，不影响本地加载。

导入：

- `CurryLocations=False`：数据/事件正确，60/67坐标。
- `CurryLocations=True`：64/67坐标，仅VEOG/HEOG/Trigger无XYZ；拓扑通过。
- 事件审计：`Experimental events: 600`、`Candidate trial pairs: 300`、`Invalid pairs: 0`。
- 频谱人工确认50 Hz尖峰；未处理。

Notion：

- Workspace：`Chain Kevin的 Notion`。
- 搜索 N400/EEGLAB handoff 未找到已有项目页，故创建独立页。
- 本地 Notion Skill 路径读取失败：`No such file or directory`。
- skill资源读取失败：`Unexpected response type`。
- Markdown spec读取失败：`Unknown resource`。
- 因此使用基础 Markdown，并创建后 fetch 验证。

## 6. 已知风险与未解决问题

1. ERPLAB 13 与 MATLAB R2026a：`version-compatibility.md:15` 记录发布说明曾警告 MATLAB 2025a+；函数能加载但全管线必须单被试 pilot。
2. 50 Hz 显著：不能未经比较固定陷波/CleanLine；最终低通可能已抑制50 Hz。
3. DC偏置/慢漂移：原始绝对值约可达 ±20–30 mV；需坏道、饱和、频谱审计。
4. 离线参考未定：候选为坏道处理后、排除VEOG/HEOG/Trigger的64 EEG平均参考；M1/M2平均乳突可作敏感性。
5. 行为CSV未提供：尚未验证300行与EEG目标事件逐trial一致、Correct值域/缺失、练习/重复/中断。
6. 现有BinList锁句子，不适用目标词；目标BinList及Correct事件标记未生成。
7. 滤波、降采样、线噪声、坏道/坏段、ICA通道、插值时点、epoch、baseline、伪迹阈值、N400窗口/ROI均保持 `[DECIDE]`。
8. 统计具体实现（RM-ANOVA或混合模型、计划比较、多重校正、缺失trial）未定。
9. Codex无Desktop写权限；所有SET由用户保存，需核实产物。
10. 无Git追踪。

## 7. 下一步最小可执行步骤

只做行为对齐，不启动预处理：

1. 将01A的 `*_cleaned_trial_data.csv` 放到 `/Users/kevinchain/Desktop/N400_project/behavior/` 并提供路径。
2. 只读检查行数、列名、Correct值域/缺失、Constraint/SNR/事件码、trial ID、练习/重复。
3. 从 `01A_imported.set` 提取300个目标码，与CSV逐行比对；不能只比较行数。
4. 输出对齐表/mismatch报告。gate通过后才制定 Phase 2 pre-ICA 参数表。
5. 若 canonical SET 未保存，先用 `CurryLocations=True` 重新导入并保存。

## 8. 不能改变的设计决策与约束

- 不删除/覆盖原始 `.cdt/.ceo/.dpo`、XML、DAT或 `Full.pdf`。
- CURRY三件套必须同目录同名。
- N400锁定111–125；11–25不能替代。
- 99静息开始、98结束，不进入N400 BinList。
- 10条件全部保留；主统计为Constraint、SNR及交互。
- Correct-only为主，all-trial为敏感性；两者均排除EEG伪迹。
- 错误trial不得在ICA前删除。
- 在线参考是专用REF；M1/M2是记录通道，不得误写为在线参考。
- DAT是厂家模板，不得宣称个体数字化。
- 示例滤波/参考/降采样/epoch/baseline/N400参数不得自动定案。
- 继续使用五类证据标签并引用原书物理页码。
- 每阶段QC gate通过后继续；先单被试pilot，再批处理。

